# CrossQuests

A simple, friendly website that spins random Bible-inspired "side quests" for you to go do, with rarity tiers named after biblical moments.

## Pages

- `index.html` — home page, spin the wheel
- `log.html` — quest log / discovery index

Open `index.html` in a browser to run it locally, no build step required.
